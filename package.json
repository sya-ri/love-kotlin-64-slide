{
  "name": "love-kotlin-64-slide",
  "private": true,
  "scripts": {
    "dev": "run-p build:watch preview",
    "preview": "serve dist",
    "build:watch": "marp slides.md -w --output dist/index.html",
    "build": "marp slides.md --html --output dist/index.html",
    "pdf": "marp slides.md --pdf --output dist/slides.pdf"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "4.3.1",
    "npm-run-all2": "8.0.4",
    "serve": "14.2.6"
  },
  "engines": {
    "node": "24.x"
  }
}
